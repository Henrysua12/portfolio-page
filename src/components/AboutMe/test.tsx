import React from "react";
type AboutMeProps = {
    aboutMe: string;
  };

  export const AboutMe = ({ aboutMe }: AboutMeProps) => (
    <div>
        <h1>
            About Me
        </h1>
    </div>

  )